# 🎨 PrintBox3D - System Architecture Overview

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     FRONTEND (React)                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Components: Shop, Custom Orders, Contact, etc.      │  │
│  │  API Service: Handles all backend communication      │  │
│  │  Deployed on: Vercel/Netlify                        │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP/HTTPS
                         │ JSON/FormData
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   BACKEND API (Django)                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Django REST Framework                               │  │
│  │  - Products API                                      │  │
│  │  - Custom Orders API                                 │  │
│  │  - Contact/Newsletter API                           │  │
│  │  - Testimonials API                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Business Logic Layer                                │  │
│  │  - Validation                                        │  │
│  │  - File Processing                                   │  │
│  │  - Data Serialization                               │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Admin Panel (Django Admin)                          │  │
│  │  - Content Management                                │  │
│  │  - Order Management                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Deployed on: Railway                                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE (PostgreSQL)                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Tables:                                             │  │
│  │  - Products                                          │  │
│  │  - Categories                                        │  │
│  │  - Materials                                         │  │
│  │  - Custom Orders                                     │  │
│  │  - Contact Messages                                  │  │
│  │  - Newsletter Subscriptions                         │  │
│  │  - Testimonials                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Hosted on: Railway (PostgreSQL Add-on)                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### 1. Product Browsing Flow

```
User Opens Shop Page
        │
        ▼
React App Requests Products
        │
        ▼
API: GET /api/products/?category=home-decor
        │
        ▼
Django Views Process Request
        │
        ▼
Database Query (PostgreSQL)
        │
        ▼
Serialize Data (DRF)
        │
        ▼
Return JSON Response
        │
        ▼
React Displays Products
```

### 2. Custom Order Submission Flow

```
User Fills Custom Order Form
        │
        ▼
User Uploads Design File (optional)
        │
        ▼
React Creates FormData Object
        │
        ▼
API: POST /api/custom-orders/
        │
        ▼
Django Validates Data
   - Check required fields
   - Validate email format
   - Check file size/type
        │
        ▼
Save to Database
   - Customer info
   - Order details
   - File to media folder
        │
        ▼
Return Success Response
        │
        ▼
React Shows Confirmation
        │
        ▼
Admin Gets Notification
(can view in Admin Panel)
```

### 3. Admin Management Flow

```
Admin Logs into Admin Panel
        │
        ▼
Views Custom Orders
        │
        ▼
Updates Order Status
   - Pending → Reviewing
   - Add quote amount
   - Add admin notes
        │
        ▼
Saves Changes
        │
        ▼
Database Updated
        │
        ▼
(Optional) Email Sent to Customer
```

---

## 🗂️ Database Schema

### Products Table
```
┌──────────────────────────────────────┐
│ Products                             │
├──────────────────────────────────────┤
│ id (PK)                              │
│ name                                 │
│ slug (unique)                        │
│ description                          │
│ price                                │
│ category_id (FK → Categories)        │
│ material_id (FK → Materials)         │
│ color                                │
│ dimensions                           │
│ weight                               │
│ image                                │
│ image_2                              │
│ image_3                              │
│ stock_quantity                       │
│ is_available                         │
│ is_featured                          │
│ meta_description                     │
│ created_at                           │
│ updated_at                           │
└──────────────────────────────────────┘
```

### Custom Orders Table
```
┌──────────────────────────────────────┐
│ Custom Orders                        │
├──────────────────────────────────────┤
│ id (PK)                              │
│ name                                 │
│ email                                │
│ phone                                │
│ material                             │
│ color                                │
│ description                          │
│ quantity                             │
│ budget                               │
│ design_file                          │
│ status                               │
│ admin_notes                          │
│ quote_amount                         │
│ created_at                           │
│ updated_at                           │
└──────────────────────────────────────┘
```

### Relationships
```
Categories (1) ──────── (Many) Products
Materials (1) ───────── (Many) Products
```

---

## 🔄 Request/Response Cycle

### Example: Get Product Detail

**1. Frontend Request:**
```javascript
fetch('http://localhost:8000/api/products/geometric-planter/')
```

**2. Django URL Routing:**
```python
urlpatterns = [
    path('products/<slug:slug>/', ProductViewSet.as_view({'get': 'retrieve'}))
]
```

**3. View Processing:**
```python
class ProductViewSet:
    def retrieve(self, request, slug):
        product = Product.objects.get(slug=slug)
        serializer = ProductDetailSerializer(product)
        return Response(serializer.data)
```

**4. Database Query:**
```sql
SELECT * FROM products 
WHERE slug = 'geometric-planter' 
AND is_available = true;
```

**5. Serialization:**
```python
{
    "id": 1,
    "name": "Geometric Planter",
    "price": "899.00",
    ...
}
```

**6. Response to Frontend:**
```
HTTP 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "Geometric Planter",
  ...
}
```

---

## 🛡️ Security Layers

```
┌─────────────────────────────────────┐
│  1. Django Security Middleware      │
│     - XSS Protection                │
│     - CSRF Protection               │
│     - Clickjacking Protection       │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│  2. CORS Middleware                 │
│     - Origin validation             │
│     - Method validation             │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│  3. DRF Permissions                 │
│     - AllowAny for public endpoints │
│     - Admin-only for sensitive data │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│  4. Input Validation                │
│     - Serializer validation         │
│     - File type/size validation     │
│     - Email validation              │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│  5. Database Layer                  │
│     - ORM prevents SQL injection    │
│     - Parameterized queries         │
└─────────────────────────────────────┘
```

---

## 📁 File Structure Visualization

```
PrintBox-Backend/
│
├── 📄 Configuration Files
│   ├── requirements.txt      (Python dependencies)
│   ├── Procfile             (Process definition)
│   ├── railway.json         (Railway config)
│   ├── runtime.txt          (Python version)
│   ├── .env.example         (Environment template)
│   └── .gitignore           (Git ignore rules)
│
├── 📚 Documentation
│   ├── README.md            (Main documentation)
│   ├── QUICKSTART.md        (Quick start guide)
│   ├── API_DOCUMENTATION.md (API reference)
│   ├── DEPLOYMENT.md        (Deployment guide)
│   ├── FRONTEND_INTEGRATION.md (React integration)
│   ├── PROJECT_SUMMARY.md   (Project overview)
│   ├── SETUP_AND_TESTING.md (Setup & testing)
│   └── DOCUMENTATION_INDEX.md (This index)
│
├── 🔧 Django Project (printbox_backend/)
│   ├── __init__.py
│   ├── settings.py          (Configuration)
│   ├── urls.py              (Root routing)
│   ├── wsgi.py              (WSGI server)
│   └── asgi.py              (ASGI server)
│
├── 🎯 API Application (api/)
│   ├── models.py            (Database models)
│   ├── serializers.py       (Data serialization)
│   ├── views.py             (API endpoints)
│   ├── urls.py              (API routing)
│   ├── admin.py             (Admin config)
│   ├── tests.py             (Unit tests)
│   └── management/
│       └── commands/
│           └── populate_sample_data.py
│
├── 📦 Static Files
│   └── staticfiles/         (Collected static files)
│
├── 📸 Media Files
│   └── media/
│       ├── products/        (Product images)
│       ├── categories/      (Category images)
│       ├── custom_orders/   (Design files)
│       └── testimonials/    (Customer photos)
│
└── 🛠️ Utilities
    ├── manage.py            (Django management)
    └── setup.py             (Setup script)
```

---

## 🌐 Deployment Architecture

```
┌─────────────────────────────────────────────────────┐
│              RAILWAY DEPLOYMENT                      │
│                                                      │
│  ┌────────────────────────────────────────────┐   │
│  │     Web Service (Django + Gunicorn)        │   │
│  │     - Handles HTTP requests                │   │
│  │     - Serves API endpoints                 │   │
│  │     - Serves static files (WhiteNoise)     │   │
│  └────────────────────────────────────────────┘   │
│                      │                              │
│                      │                              │
│  ┌────────────────────────────────────────────┐   │
│  │     PostgreSQL Database                    │   │
│  │     - Stores all application data          │   │
│  │     - Automatic backups                    │   │
│  │     - Connection pooling                   │   │
│  └────────────────────────────────────────────┘   │
│                                                      │
│  Environment Variables:                             │
│  - SECRET_KEY                                       │
│  - DEBUG=False                                      │
│  - DATABASE_URL (auto-set)                         │
│  - ALLOWED_HOSTS                                    │
│  - CORS_ALLOWED_ORIGINS                            │
│                                                      │
└─────────────────────────────────────────────────────┘
                      │
                      │ HTTPS
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│              FRONTEND (Vercel/Netlify)              │
│                                                      │
│  - React Application                                │
│  - Connects to Railway API                         │
│  - Serves static files from CDN                    │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 Development Workflow

```
┌─────────────────────────────────────────────┐
│  1. Local Development                        │
│     - SQLite database                        │
│     - DEBUG=True                             │
│     - python manage.py runserver             │
└─────────────────────────────────────────────┘
               │
               │ git commit & push
               ▼
┌─────────────────────────────────────────────┐
│  2. GitHub Repository                        │
│     - Version control                        │
│     - Code review                            │
│     - Collaboration                          │
└─────────────────────────────────────────────┘
               │
               │ railway up
               ▼
┌─────────────────────────────────────────────┐
│  3. Railway Deployment                       │
│     - Automatic build                        │
│     - Run migrations                         │
│     - Collect static files                   │
│     - Start gunicorn                         │
└─────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  4. Production                               │
│     - PostgreSQL database                    │
│     - DEBUG=False                            │
│     - HTTPS enabled                          │
│     - Monitoring active                      │
└─────────────────────────────────────────────┘
```

---

## 🎯 API Endpoint Map

```
/api/
│
├── /products/
│   ├── GET  /                    (List all products)
│   ├── GET  /{slug}/             (Product detail)
│   ├── GET  /featured/           (Featured products)
│   └── GET  /best_sellers/       (Best sellers)
│
├── /categories/
│   ├── GET  /                    (List categories)
│   └── GET  /{slug}/             (Category detail)
│
├── /materials/
│   ├── GET  /                    (List materials)
│   └── GET  /{id}/               (Material detail)
│
├── /custom-orders/
│   └── POST /                    (Submit custom order)
│
├── /contact/
│   └── POST /                    (Submit contact message)
│
├── /newsletter/
│   └── POST /                    (Subscribe to newsletter)
│
└── /testimonials/
    ├── GET  /                    (List testimonials)
    └── GET  /featured/           (Featured testimonials)
```

---

## 🔍 Monitoring & Logging

```
┌─────────────────────────────────────────────┐
│  Application Logs                            │
│  - Request/Response logs                     │
│  - Error logs                                │
│  - Database query logs                       │
└─────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Railway Dashboard                           │
│  - Real-time logs                            │
│  - Deployment history                        │
│  - Resource usage                            │
│  - Metrics & analytics                       │
└─────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Alerts & Notifications                      │
│  - Deployment failures                       │
│  - High error rates                          │
│  - Resource limits                           │
└─────────────────────────────────────────────┘
```

---

## 📊 Performance Optimization

```
┌─────────────────────────────────────────────┐
│  Database Level                              │
│  - Indexed fields (slug, category, material)│
│  - Connection pooling                        │
│  - Query optimization                        │
└─────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Application Level                           │
│  - Efficient serialization                   │
│  - Pagination                                │
│  - Filtered queries                          │
└─────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Server Level                                │
│  - Gunicorn workers                          │
│  - Static file compression (WhiteNoise)      │
│  - GZIP compression                          │
└─────────────────────────────────────────────┘
```

---

## 🎨 Complete System Overview

```
┌───────────────────────────────────────────────────────────┐
│                     USER INTERACTION                       │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  Browser/Mobile → React App → API Calls             │ │
│  └─────────────────────────────────────────────────────┘ │
└────────────────────────┬──────────────────────────────────┘
                         │
                    HTTP/HTTPS
                         │
┌────────────────────────▼──────────────────────────────────┐
│                   DJANGO REST API                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │ URL Routing → Views → Business Logic → Serializers │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────────┬──────────────────────────────────┘
                         │
                    ORM Queries
                         │
┌────────────────────────▼──────────────────────────────────┐
│                   POSTGRESQL DATABASE                      │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Products | Categories | Orders | Contacts | etc.  │  │
│  └────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

---

**This visual overview helps understand how all components work together! 🎉**

*For detailed information, refer to specific documentation files.*
